# redis连接配置
spring:
  redis:
    host: 127.0.0.1
    port: 6379
    timeout: 60000
    jedis:
      pool:
        maxActive: 8
        maxIdle: 8
        minIdle: 0
        max-wait: 5000
    database: 0 # 指定数据库1